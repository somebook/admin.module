%h1= t("admin.shard.listing")

= render partial: "shared/flash_messages"

%br

%table.table.table-striped
  %tr
    %th= Shard.model_name.human
    %th= t("global.actions")

  - @shards.each do |shard|
    %tr
      %td{ class: "name" }
        = shard.name
      %td{ class: "operations"}
        = link_to t("admin.shard.users"), shard_shard_users_path(shard), class: %w[ btn btn-mini ]
        = link_to t("admin.shard.languages"), shard_shard_languages_path(shard), class: %w[ btn btn-mini ]
        = link_to t("global.edit"), edit_shard_path(shard), class: %w[ btn btn-mini ]
        = link_to t("global.destroy"), shard,
          data: { confirm: t("global.delete_confirm") },
          method: :delete,
          class: %w[ btn btn-mini btn-danger ]

= link_to t("admin.shard.new"), new_shard_path, class: %w[ btn btn-primary ]
